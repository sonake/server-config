spring:
  redis:
    host: 127.0.0.1
    port: 6379
    password:
    jedis:
      pool:
        min-idle: 8
        max-idle: 500
        max-active: 2000
        max-wait: 10000
    timeout: 0
    database: 1
  datasource:
    dynamic:
      # 是否开启 SQL日志输出，生产环境建议关闭，有性能损耗
      p6spy: true
      hikari:
        connection-timeout: 30000
        max-lifetime: 1800000
        max-pool-size: 15
        min-idle: 5
        connection-test-query: select 1
        pool-name: HcHikariCP
      # 配置默认数据源
      primary: primary
      datasource:
        # 数据源-1，名称为 primary
        primary:
          username: root
          password: Jdkc0912..
          driverClassName: com.mysql.cj.jdbc.Driver
          url: jdbc:mysql://47.105.40.52:3306/sonake?useUnicode=true&characterEncoding=UTF-8&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC&useSSH=false
        # 数据源-2，名称为 test
mybatis-plus:
  mapperLocations: classpath:mapper/**/*.xml
  type-aliases-package: com.hc.admim.bean
  global-config:
    banner: false

pagehelper:
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql

#数据库类型
renren:
  database: mysql
#自定义配置
hc:
  shiro:
    # token有效期，单位秒
    tokenTimeOut: 36000
    # 免签url
    anonUrl: /login,/logout/**,/regist,/user/check/**